package mochi

/**
 * Created by elberry on 12/5/16.
 */
enum EncryptionPluginSettings {
	// encrypt on save - default is to encrypt and delete decrypted file when file is closed.
	// master password file location - defaults to [plugin.home]/passwords.txt.jenc
	// optional: Decrypt files to random directory
}
